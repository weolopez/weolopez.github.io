name: Update Site Copy

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: POST done-screen copy
        run: |
          curl -s -X POST https://worldcup.weolopez.com/worldcup/api/site-copy/done-screen \
            -H "X-Cron-Token: 9ed183946a2fa8a0563160feabd809d0cbc506831b596660" \
            -H "Content-Type: application/json" \
            -d '{"trophy":"🇳🇱","title":"DUTCH MASTERS MEET THE SAMURAI! 4PM ET","subtitle":"5 fans knotted at 5 pts — Sean, Jeremy & jeffhuberty all got Mexico. Can you catch them?"}'
      - name: Verify
        run: |
          curl -s https://worldcup.weolopez.com/worldcup/api/site-copy/done-screen
